# Database
MONGODB_URI=mongodb://localhost:27017/omyra-project-nexus

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server
PORT=5000
NODE_ENV=development

# CORS
FRONTEND_URL=http://localhost:5173

# Email Configuration
# Choose your email provider and configure accordingly

# Gmail Configuration (Recommended for development)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@omyra-project.com

# Alternative Email Providers
# Uncomment and configure the provider you want to use:

# SendGrid Configuration
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# Mailgun Configuration
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-mailgun-smtp-username
# EMAIL_PASSWORD=your-mailgun-smtp-password

# AWS SES Configuration
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-aws-ses-smtp-username
# EMAIL_PASSWORD=your-aws-ses-smtp-password

# Development SMTP Server Configuration (for testing)
SMTP_USER=admin
SMTP_PASSWORD=password123
SMTP_PORT=2525

# Instructions for Gmail Setup:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate an app password for "Mail"
# 4. Use the generated password in EMAIL_PASSWORD
# 5. Use your full Gmail address in EMAIL_USER

# Built-in SMTP Server Configuration (for receiving emails)
SMTP_USER=admin
SMTP_PASSWORD=password123
SMTP_PORT=2525

# Optional: Email Service Providers Configuration
# For SendGrid
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# For Mailgun
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USER=your-mailgun-username
# EMAIL_PASSWORD=your-mailgun-password

# For AWS SES
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_USER=your-ses-access-key
# EMAIL_PASSWORD=your-ses-secret-key
